---
type Item = { q: string; a: string };
type Props = { items: Item[] };
const { items } = Astro.props;
---

<div class="accordion">
	{
		items.map((item) => (
			<details class="acc">
				<summary>{item.q}</summary>
				<p>{item.a}</p>
			</details>
		))
	}
</div>
