# Cloudflare Pages / Wrangler
# Dashboard: Build-Befehl `npm run build`, Ausgabeverzeichnis `dist`.
# Kein eigener Worker fuer das Kontaktformular (Web3Forms, siehe README).

name = "nineto6"
compatibility_date = "2025-09-18"
pages_build_output_dir = "./dist"
